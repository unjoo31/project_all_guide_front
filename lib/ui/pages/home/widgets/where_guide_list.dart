import 'package:flutter/material.dart';

class WhereGuideList extends StatelessWidget {
  const WhereGuideList({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text("WhereGuideList"),
      ),
    );
  }
}
