import 'package:flutter/material.dart';

class WhoGuideList extends StatelessWidget {
  const WhoGuideList({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text("WhoGuideList"),
      ),
    );
  }
}
