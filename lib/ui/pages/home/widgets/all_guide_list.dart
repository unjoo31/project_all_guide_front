import 'package:flutter/material.dart';

class AllGuideList extends StatelessWidget {
  const AllGuideList({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text("AllGuideList"),
      ),
    );
  }
}
