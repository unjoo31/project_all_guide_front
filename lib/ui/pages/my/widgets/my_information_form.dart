import 'package:flutter/material.dart';

class MyInformationForm extends StatelessWidget {
  const MyInformationForm({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text("MyInformationForm"),
      ),
    );
  }
}
