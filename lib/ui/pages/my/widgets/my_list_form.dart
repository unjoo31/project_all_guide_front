import 'package:flutter/material.dart';

class MyListForm extends StatelessWidget {
  const MyListForm({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text("MyListForm"),
      ),
    );
  }
}
