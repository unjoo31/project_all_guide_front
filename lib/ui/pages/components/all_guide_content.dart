import 'package:flutter/material.dart';

class AllGuideContent extends StatelessWidget {
  const AllGuideContent({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
